get_filename_component(CDSS_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

if(NOT TARGET @LIBRARY_NAME@ AND NOT CDSS_BINARY_DIR)
  include("${CDSS_CMAKE_DIR}/CDSSTargets.cmake")
endif()

find_package(Threads REQUIRED)

set(CDSS_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")
set(CDSS_LIBRARIES "@LIBRARY_NAME@" ${CMAKE_THREAD_LIBS_INIT})
set(CDSS_LIBRARIES_STATIC "@LIBRARY_NAME_STATIC@" ${CMAKE_THREAD_LIBS_INIT})
